import { test, expect } from '@playwright/test';

/*
|--------------------------------------------------------------------------
| DRAG AND DROP
|--------------------------------------------------------------------------
| Escenario:
| - Arrastramos un elemento
| - Lo soltamos en un target
| - Validamos que el drop fue exitoso
*/
test('drag element to target', async ({ page }) => {
  // TODO: Implementar la lógica del test
  // - Navegar a la página
  // - Localizar source y target
  // - Ejecutar drag and drop
  // - Validar resultado
});
